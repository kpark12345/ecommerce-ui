import { connect } from "react-redux";
import Body from "./../components/Body.jsx";

var mapStateToProps = store => ({
  
});

var mapDispatchToProps = dispatch => ({
  
});

var BodyContainer = connect(
  mapStateToProps,
  mapDispatchToProps
)(Body);

export default BodyContainer;